# Font Asset

Place your locked Chinese title font here as:

```text
title-font.ttf
```

This file is intentionally not included in the public repository. The Pillow renderer must stop if the configured font is missing.
